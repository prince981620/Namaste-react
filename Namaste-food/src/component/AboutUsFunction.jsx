export const AboutUsFunction = ({name,location,contact})=>{
    return (
    <div className="border border-black m-2 p-5">
        <div>{name}</div>
        <div>{location}</div>
        <div>{contact}</div>
    </div>
)}